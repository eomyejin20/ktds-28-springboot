<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace? Mybatis가 실행해야하는 쿼리의 클래스의 경로를 작성. 해당 클래스의 메소드와 mapper에 작성하는 
	쿼리가 매칭되어서 실행된다. Mybatis가 생성하는 익명클래스의 원본 -->
<mapper
	namespace="com.ktdsuniversity.edu.articles.dao.ArticlesDao">

	<!-- ArticlesDao 인터페이스에 있는 추상메소드들을 구현 -->
	<!-- long selectArticlesCount(); <select id="selectArticlesCount" 메소드의 이름 -->
	<!-- parameterType="" 메소드의 파라미터 타입, 없으면 생략 -->
	<!-- resultType="long" 메소드의 파라미터 타입 -->
	<!-- </select> -->

	<select id="selectArticlesCount" resultType="_long">
		SELECT COUNT(ID)
		FROM ARTICLES
		WHERE DEL_YN = 'N'
	</select>

	<!-- List <ArticlesVO> selectAllArticles(); -->

	<select id="selectAllArticles"
		resultType="com.ktdsuniversity.edu.articles.vo.response.ArticlesVO">
		SELECT ID
		, SUBJECT
		, CONTENT
		, EMAIL
		, VIEW_CNT
		, RECOMMEND_CNT
		, DEL_YN
		, TO_CHAR(CRT_DT, 'YYYY-MM-DD HH24:MI:SS') AS CRT_DT
		,
		TO_CHAR(MDFY_DT, 'YYYY-MM-DD HH24:MI:SS') AS MDFY_DT
		, FILE_SET_ID
		FROM ARTICLES
		WHERE DEL_YN = 'N'
	</select>

	<insert id="insertNewArticle"
		parameterType="com.ktdsuniversity.edu.articles.vo.request.RegistArticleVO">

		<selectKey order="BEFORE" keyProperty="id"
			resultType="string">
			SELECT 'AR-' || TO_CHAR(SYSDATE , 'YYYYMMDD-') ||
			LPAD(SEQ_ARTICLES_PK.NEXTVAL, 6,
			'0')
			FROM DUAL
		</selectKey>
		INSERT INTO ARTICLES
		(ID
		, SUBJECT
		, CONTENT
		, EMAIL
		, CRT_DT
		, FILE_SET_ID)
		VALUES
		(#{id}
		, #{subject}
		, #{content}
		, #{email}
		, SYSDATE
		, NULL)
	</insert>
	<select id="selectArticleByArticleId" parameterType="string"
		resultType="com.ktdsuniversity.edu.articles.vo.response.ArticlesVO">
		SELECT ID
		, SUBJECT
		, CONTENT
		, EMAIL
		, VIEW_CNT
		, RECOMMEND_CNT
		, DEL_YN
		, TO_CHAR(CRT_DT, 'YYYY-MM-DD HH24:MI:SS') AS CRT_DT
		,
		TO_CHAR(MDFY_DT, 'YYYY-MM-DD HH24:MI:SS') AS MDFY_DT
		, FILE_SET_ID
		FROM
		ARTICLES
		WHERE ID = #{_parameter}
		AND DEL_YN = 'N'
	</select>


	<update id="updateArticleId">
		UPDATE ARTICLES
		SET SUBJECT =#{modifyArticleVO.subject}
		, CONTENT =#{modifyArticleVO.content}
		, MDFY_DT = SYSDATE
		WHERE ID = #{articleId}
		AND EMAIL = #{modifyArticleVO.email}
		AND DEL_YN = 'N'
	</update>

	<update id="deleteArticle" parameterType="string">
		UPDATE ARTICLES
		SET DEL_YN = 'Y'
		, MDFY_DT = SYSDATE
		WHERE ID = #{_parameter}
		AND DEL_YN = 'N'
	</update>
</mapper>